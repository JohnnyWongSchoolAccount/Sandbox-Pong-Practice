/* known ERRORs
 - To be implemented
*/
class Paddle {
  //Global Variables
  float playAreaWidth, playAreaHeight, playAreaX, playAreaY;
  float knotX, knotY, knotWidth, knotHeight;
  float paddleX, paddleY, paddleWidth, paddleHeight;
  color paddleColor
  //
  Paddle(float startPosition) {
    paddleX = ;
    paddleY = ;
    paddleWidth = ;
    paddleHeight = ;
    paddleColor = color(int(random(0, 255)), int(random(0,255)), int(random(0,255)));
  }//end paddle
  void drawPaddle() {
  }
}//end Paddle
//end pongPaddle
